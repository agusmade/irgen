import type { FrontendIR } from "../domain/frontend.js";

/**
 * TargetIR for frontend emitters. Mirrors FrontendIR for now
 * but isolates emitter-facing shape.
 */
export type FrontendTargetIR = FrontendIR;

